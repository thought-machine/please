package core

// sandboxDir is unused for now - there is no sandbox on Windows (see M7 in
// docs/design/windows) - but it still has to be a path that never collides with a repo.
const sandboxDir = `C:\plz_sandbox`
