subinclude("//build_defs:custom_build_def")

custom_target(
    name = "custom_target",
    srcs = ["file.txt"],
    outs = ["file.out"],
    outs_adjacent = ["file_adjacent.out"],
)

custom_target(
    name = "unneeded",
    srcs = ["file.txt"],
    outs = ["unneeded.out"],
    outs_adjacent = ["unneeded_adjacent.out"],
)
