genrule(
    name = "glob_target",
    srcs = glob(["*.txt"], exclude = ["ignored.txt"]),
    outs = ["out.txt"],
    cmd = "cat $SRCS > $OUT",
)

genrule(
    name = "unused",
    srcs = ["unused.dat"],
    outs = ["unused.out"],
    cmd = "cat $SRCS > $OUT",
)
