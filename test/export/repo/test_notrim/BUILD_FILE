# BUILD_STMT_START target1
genrule(
    name = "target1",
    srcs = ["file1.txt"],
    outs = ["out1.txt"],
    cmd = "cat $SRCS > $OUT",
)
# BUILD_STMT_END

# BUILD_STMT_START unrelated
genrule(
    name = "unrelated",
    srcs = ["file2.txt"],
    outs = ["out2.txt"],
    cmd = "cat $SRCS > $OUT",
)
# BUILD_STMT_END