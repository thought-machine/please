# BUILD_STMT_START dep1
genrule(
    name = "dep1",
    srcs = ["file1.txt"],
    outs = ["out1.txt"],
    cmd = "cat $SRCS > $OUT",
)
# BUILD_STMT_END

# BUILD_STMT_START dep2
genrule(
    name = "dep2",
    srcs = [
        "file2.txt",
        ":dep1",
        "//test_deps_other:other_target",
    ],
    outs = ["out2.txt"],
    cmd = "cat $SRCS > $OUT",
)
# BUILD_STMT_END
