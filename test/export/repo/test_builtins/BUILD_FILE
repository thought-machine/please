# BUILD_STMT_START native_genrule
genrule(
    name = "native_genrule",
    srcs = ["file.txt"],
    outs = ["file.wordcount"],
    cmd = "wc $SRCS > $OUT",
)
# BUILD_STMT_END

# Du mmy target to be trimmed
genrule(
    name = "dummy",
    srcs = ["file.txt"],
    outs = ["dummy"],
    cmd = "cat $SRCS > $OUT",
)
