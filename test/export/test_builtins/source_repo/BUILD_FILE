genrule(
    name = "native_genrule",
    srcs = ["file.txt"],
    outs = ["file.wordcount"],
    cmd = "$TOOLS $SRCS > $OUT",
    tools = ["//tools:tool"],
)

genrule(
    name = "unused_target_to_be_trimmed",
    srcs = ["unused.txt"],
    outs = ["unused"],
    cmd = "cat $SRCS > $OUT",
)
