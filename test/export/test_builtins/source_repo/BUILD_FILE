genrule(
    name = "native_genrule",
    srcs = ["file.txt"],
    outs = ["file.wordcount"],
    cmd = "$TOOLS $SRCS > $OUT",
    tools = ["//tools:tool"],
)

genrule(
    name = "unneeded_target_to_be_trimmed",
    srcs = ["unneeded.txt"],
    outs = ["unneeded"],
    cmd = "cat $SRCS > $OUT",
)
