genrule(
    name = "native_genrule",
    srcs = ["file.txt"],
    outs = ["file.wordcount"],
    cmd = "$TOOLS $SRCS > $OUT",
    tools = ["//tools:tool"],
)

genrule(
    name = "dummy_target_to_be_trimmed",
    srcs = ["dummy.txt"],
    outs = ["dummy"],
    cmd = "cat $SRCS > $OUT",
)
