genrule(
    name = "native_genrule",
    srcs = ["file.txt"],
    outs = ["file.wordcount"],
    cmd = "$TOOLS $SRCS > $OUT",
    tools = ["//tools:tool"],
)

genrule(
    name = "unneded_target_to_be_trimmed",
    srcs = ["unneded.txt"],
    outs = ["unneded"],
    cmd = "cat $SRCS > $OUT",
)
