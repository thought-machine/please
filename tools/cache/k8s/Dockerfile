# N.B. for this to work you'll need a completely static RPC server binary.
#      You can try that by setting static = True on it although that may cause issues
#      with cgo for DNS resolution.
#      The images in our repository are built fully statically so avoid that problem.
FROM scratch

COPY /rpc_cache_server /rpc_cache_server
