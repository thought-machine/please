for i in len(x):
    i.replace('-', '_')
    assert subinclude("//build_defs:fpm")

if x.find('blah'):
    return subinclude("//build_defs:fpm")
elif x.count('blah'):
    return x.replace('-', '_')
else:
    return x.lower()
